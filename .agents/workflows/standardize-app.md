---
description: Standardize a single app on the latest narduk-nuxt-template — reads full instructions from .agents/app-standardization/standardize-app.md
---

# Standardize App

This workflow lives in `.agents/app-standardization/standardize-app.md`. Read it now:

```bash
cat .agents/app-standardization/standardize-app.md
```

Before starting, also read the tracking file:

```bash
cat .agents/app-standardization/STANDARDIZATION.md
```
