{
  "name": "nuxt-v4-template-monorepo",
  "private": true,
  "scripts": {
    "dev": "doppler run -- pnpm -r --filter web dev",
    "build": "doppler run -- pnpm -r build",
    "build:plugins": "pnpm --filter @narduk/eslint-config build",
    "lint": "doppler run -- pnpm -r lint",
    "typecheck": "doppler run -- pnpm -r typecheck",
    "quality": "doppler run -- pnpm -r quality",
    "setup": "doppler run -- jiti tools/init.ts",
    "validate": "doppler run -- jiti tools/validate.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@parcel/watcher",
      "better-sqlite3",
      "core-js",
      "esbuild",
      "protobufjs",
      "sharp",
      "vue-demi",
      "workerd"
    ]
  },
  "volta": {
    "node": "22.22.0"
  }
}
